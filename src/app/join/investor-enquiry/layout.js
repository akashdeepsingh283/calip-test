import { generatePageMetadata } from "../../lib/seo";

export const metadata = generatePageMetadata("investor-enquiry-join");

export default function InvestorEnquiryJoinLayout({ children }) {
  return children;
}
